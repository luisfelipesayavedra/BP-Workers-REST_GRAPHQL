/* import { TYPE_DNI } from "../../../../../generated/client" */

export interface noteDebitPurchaseExcel {
  numero: string;
  fecha_creacion: string;
  bodega: string;
  factura: string;
  cliente_type_dni: string;
  provider_dni: string;
  item_name: string;
  item_quantity: string;
  total_note: string;
  saldo_pagar: string;
}